<% provide(:title, 'Create a home') %>
<h1>Create a home</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@home) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Create home", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>
